import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ibirdies.demogooglemaps',
  appName: 'DemoGoogleMaps',
  webDir: 'dist'
};

export default config;
